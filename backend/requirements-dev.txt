# Development/testing dependencies (not needed in production).
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
pytest==8.3.4
httpx==0.27.2   # already in requirements.txt; FastAPI TestClient needs it
